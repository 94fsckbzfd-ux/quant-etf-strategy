# 📈 V8.7 ETF动量轮动策略 - Manus Agent交接文档

> **文档目的**：本文档旨在完整记录“从策略评估到自动化部署”的全过程，以便后续任何AI Agent（如Telegram Bot）能够无缝衔接，理解项目背景、系统状态和操作流程。

---

## 1. 项目核心目标

将用户提供的V8.7 ETF轮动策略（手动版）部署为一套全自动、云端运行的信号生成系统，并在每周五自动提醒用户进行交易。

## 2. 系统架构

- **代码托管**：私有GitHub仓库 `94fsckbzfd-ux/quant-etf-strategy`
- **自动化核心**：GitHub Actions
- **运行逻辑**：
  1. 每周五14:45（北京时间）自动触发工作流。
  2. 拉取最新代码和持仓状态。
  3. 安装Python依赖（`pandas`, `numpy`, `akshare`, `requests`）。
  4. 运行 `v8_live_signal.py` 脚本生成交易信号。
  5. 提取信号摘要，并通过邮件发送给用户。
  6. 将更新后的持仓文件 `portfolio_state.json` 自动提交回仓库。

## 3. 关键文件清单

| 文件路径 | 用途说明 |
| :--- | :--- |
| `v8_live_signal.py` | **策略主引擎**：负责数据获取、信号计算、订单生成。 |
| `v8_live_config.py` | **策略配置文件**：定义所有可调参数（如动量周期、资产池等）。 |
| `portfolio_state.json` | **账户状态文件**：记录当前现金、持仓、净值等信息，每次运行后自动更新。 |
| `.github/workflows/weekly_signal.yml` | **GitHub Actions工作流**：定义自动化流程的每一步。 |
| `README.md` | **项目说明书**：提供给人类阅读的快速上手指南。 |

## 4. 当前系统状态 (截至2026-03-11)

- **初始资金**：**50,000.00 人民币**
- **当前持仓**：**空仓**
- **账户状态文件**：`portfolio_state.json` 已重置为5万元现金、空仓状态。
- **首次建仓计划**：用户准备在本周五（2026-03-14）根据系统信号进行首次建仓。

## 5. 标准操作流程 (SOP)

1. **接收信号**：周五14:45左右，用户会收到来自GitHub Actions的邮件，内含交易信号摘要。
2. **人工核验 (关键)**：如果信号是`CRISIS`或`HALF`，需按策略手册要求，**人工核验外盘（如SPX）走势**，判断是“真危机”还是“假摔”，以决定是否否决避险指令。
3. **执行交易**：根据最终决策，在券商App上手动完成买卖操作。
4. **仓位同步**：无需手动操作。系统会在下一次运行时自动读取最新的持仓情况。

## 6. 关键决策与历史记录

- **首次信号分析 (2026-03-11)**：
  - 系统发出 `CRISIS` 信号，建议100%买入国债（511260）。
  - Agent通过Yahoo Finance API进行外盘核验，发现SPX跌幅不大但VIX恐慌指数快速攀升。
  - **最终结论**：CRISIS信号属实，建议用户遵循系统指令，首次建仓买入国债。

- **GitHub Actions调试**：
  - **问题**：首次测试时邮件内容为空。
  - **原因**：工作流文件中邮件发送步骤的 `body_file` 参数已过时。
  - **修复**：修改工作流，将信号摘要读入环境变量，再通过 `body` 参数传入邮件，问题解决。

- **`portfolio_state.json`重置**：
  - **问题**：Agent在沙箱中测试时，污染了 `portfolio_state.json` 文件，写入了模拟持仓。
  - **修复**：在用户首次建仓前，已将该文件手动重置为5万元现金、空仓的初始状态，并推送至GitHub仓库。

---

**交接建议**：后续Agent可通过读取此文档和GitHub仓库中的文件，完全掌握项目状态。当用户在Telegram上查询时，Agent应能：
1.  访问GitHub仓库，读取 `signal_summary.txt` 获取最新信号。
2.  结合 `HANDOVER_LOG.md` 中的历史背景，向用户提供带有上下文的、智能化的操作建议。
